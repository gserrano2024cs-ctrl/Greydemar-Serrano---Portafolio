<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Greydemar Serrano | Front-End Developer, Social Media & Project Manager</title>
  <meta name="title" content="Greydemar Serrano | Front-End Developer, Social Media & Project Manager">
  <meta name="description" content="Front-End Developer (HTML5, CSS3, Vanilla JS, Astro), especialista en Social Media y Project Manager. Interfaces rápidas, contenido que convierte y operaciones sin fricción.">

  <!-- FAVICON: "G" en perspectiva -->
  <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><rect width=%22100%22 height=%22100%22 fill=%22%230f172a%22 rx=%2212%22/><g transform=%22skewX(-15)%22><text x=%2260%22 y=%2272%22 font-size=%2265%22 font-weight=%22900%22 font-family=%22sans-serif%22 fill=%22%23ffffff%22 text-anchor=%22middle%22>G</text></g></svg>">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">

  <!-- Marca el documento como "con JS" para que las animaciones de entrada solo se oculten si el script puede revelarlas -->
  <script is:inline>document.documentElement.classList.add('js');</script>
</head>
<body>

  <!-- SISTEMA DE ZOOM GLOBAL -->
  <div id="zoom-overlay" class="zoom-overlay" onclick="closeZoom(event)">
    <button class="zoom-close" onclick="closeZoom(event)" aria-label="Cerrar">✕</button>
    <div class="zoom-content-wrapper">
      <div id="zoom-media-container"></div>
      <p id="zoom-caption"></p>
    </div>
  </div>

  <!-- FONDO ESTÁTICO GLOBAL -->
  <div class="global-fixed-bg">
    <svg class="sticker sticker-star" width="45" height="45" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <polyline points="3 3 3 21 21 21" stroke="#94a3b8" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
      <polyline points="3 17 9 11 13 15 21 7" stroke="#94a3b8" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
      <polyline points="14 7 21 7 21 14" stroke="#94a3b8" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
    <svg class="sticker sticker-plant" width="65" height="65" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <rect x="2" y="3" width="20" height="14" rx="2" stroke="#64748b" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      <path d="M8 21h8" stroke="#64748b" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      <path d="M12 17v4" stroke="#64748b" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      <polyline points="8 8 5 11 8 14" stroke="#64748b" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      <polyline points="16 8 19 11 16 14" stroke="#64748b" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
    <svg class="sticker sticker-sparkle" width="45" height="45" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M14.5 4h-5L7 7H4a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2h-3l-2.5-3z" stroke="#cbd5e1" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      <circle cx="12" cy="13" r="3" stroke="#cbd5e1" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
  </div>

  <!-- 1. SECCIÓN HERO -->
  <section class="hero-wrapper fade-in-section">
    <div class="hero-video-container">
      <!--
        IDEA DE FONDO: si prefieres una imagen limpia en lugar del video, reemplaza el <video> por:
        <img src="/hero-bg.webp" alt="" class="video-bg" />
      -->
      <video autoplay loop muted playsinline class="video-bg"><source src="/avila.webm" type="video/webm" /></video>
      <div class="avila-overlay"></div>
    </div>

    <header class="top-header">
      <div class="brand-centered">GREY SERRANO®</div>
      <div class="lang-switch dark-mode">
        <button class="lang-btn active" data-lang="es" onclick="setLanguage('es')">ES</button>
        <button class="lang-btn" data-lang="en" onclick="setLanguage('en')">EN</button>
      </div>
    </header>

    <main class="hero-container">
      <div class="left-col">
        <!-- TARJETA PERFIL · LIQUID GLASS -->
        <div class="glass-profile-card liquid-glass" data-toggle="active">
          <div class="card-min-header">
            <img src="/grey.jpeg" alt="Greydemar Serrano" class="profile-thumb shift-right" id="heroProfileImg" data-caption="Greydemar Serrano" onclick="openProfileZoom(event)" title="Ver a detalle" />
            <div class="min-meta">
              <div class="status-line"><span class="green-dot"></span><span class="status-text" data-es="En línea" data-en="Online">En línea</span></div>
              <div class="name-group">
                <h2 class="min-name">GREYDEMAR SERRANO</h2>
                <span class="min-title" data-es="Especialista en Marketing Digital y Conversión Web" data-en="Digital Marketing & Web Conversion Specialist">Especialista en Marketing Digital y Conversión Web</span>
              </div>
            </div>
            <svg class="card-chevron" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="6 9 12 15 18 9"></polyline></svg>
          </div>
          <div class="card-expand">
            <div class="card-expand-inner">
              <div class="card-expand-body">
                <p class="expand-bio" data-es="<strong>Marketing para atraer + Ventas para facturar + Web para escalar.</strong><br><br>Un gusto, soy Greydemar Serrano.<br><br>Construyo ecosistemas rentables con contenido de alto impacto, sistemas de cierre comercial y plataformas ultrarrápidas.<br><br><strong>Mi objetivo:</strong> eliminar la fricción, posicionar tu autoridad y multiplicar tus conversiones." data-en="<strong>Marketing to attract + Sales to invoice + Web to scale.</strong><br><br>Nice to meet you, I'm Greydemar Serrano.<br><br>I build profitable ecosystems with high-impact content, commercial closing systems, and ultra-fast platforms.<br><br><strong>My goal:</strong> eliminate friction, position your authority, and multiply your conversions.">
                  <strong>Marketing para atraer + Ventas para facturar + Web para escalar.</strong><br><br>Un gusto, soy Greydemar Serrano.<br><br>Construyo ecosistemas rentables con contenido de alto impacto, sistemas de cierre comercial y plataformas ultrarrápidas.<br><br><strong>Mi objetivo:</strong> eliminar la fricción, posicionar tu autoridad y multiplicar tus conversiones.
                </p>
                <p class="skills-title" data-es="Mis habilidades:" data-en="My skills:">Mis habilidades:</p>
                <div class="skills-grid">
                  <span>Astro</span><span>GitHub</span><span>HTML5</span><span>CSS3</span>
                  <span>GA4</span><span>Salesforce</span><span>Zoho CRM</span><span>Mailchimp</span>
                  <span class="wide-skill" data-es="Creación de Contenido" data-en="Content Creation">Creación de Contenido</span>
                </div>
              </div>
            </div>
          </div>
        </div>
</div>

      <div class="hero-center-text">
        <h1 data-es="ESTRATEGIA QUE UNE MARKETING, VENTAS Y WEB" data-en="STRATEGY BRIDGING MARKETING, SALES & WEB">ESTRATEGIA QUE UNE MARKETING, VENTAS Y WEB</h1>
        <span class="floating-pill pill-pink">Growth Marketing</span>
        <span class="floating-pill pill-green">Project Manager</span>
        <span class="floating-pill pill-yellow" data-es="Contenido Visual" data-en="Visual Content">Contenido Visual</span>
      </div>

      <!-- TARJETA ÁREAS · LIQUID GLASS -->
      <div class="glass-cases-card liquid-glass" data-toggle="active">
        <div class="cases-min-header">
          <span class="cases-title-text" data-es="Áreas de Expertise" data-en="Areas of Expertise">Áreas de Expertise</span>
          <span class="search-icon-wrapper"><svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg></span>
        </div>
        <div class="card-expand">
          <div class="card-expand-inner">
            <div class="card-expand-body">
              <ul class="quick-cases-list">
                <li><a href="#frontend" onclick="event.stopPropagation()"><strong>Front-End Developer</strong><span data-es="Interfaces de alto rendimiento" data-en="High-performance interfaces">Interfaces de alto rendimiento</span></a></li>
                <li><a href="#social" onclick="event.stopPropagation()"><strong>Social Media</strong><span data-es="Contenido & Growth +604%" data-en="Content & Growth +604%">Contenido & Growth +604%</span></a></li>
                <li><a href="#pm" onclick="event.stopPropagation()"><strong data-es="Ventas / Project Manager" data-en="Sales / Project Manager">Ventas / Project Manager</strong><span data-es="CRM, Logística & Operaciones" data-en="CRM, Logistics & Operations">CRM, Logística & Operaciones</span></a></li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </main>
  </section>

  <!-- 1.5 LOGO MARQUEE INFINITO -->
  <section class="marquee-section reveal">
    <div class="marquee-container">
      <div class="marquee-track">
        <img src="/logo plano creativo.png" alt="Plano Creativo"><img src="/logo dislocal.png" alt="Dislocal"><img src="/logo plano mariano moreno.png" alt="Instituto Superior Mariano Moreno"><img src="/logo impaktemos.png" alt="Impaktemos"><img src="/logo universidad.png" alt="Universidad"><img src="/logo yprealty.png" alt="YP Realty"><img src="/logo eurobuilding.png" alt="Eurobuilding Hotels"><img src="/logo panhouse.png" alt="PanHouse">
        <img src="/logo plano creativo.png" alt="Plano Creativo"><img src="/logo dislocal.png" alt="Dislocal"><img src="/logo plano mariano moreno.png" alt="Instituto Superior Mariano Moreno"><img src="/logo impaktemos.png" alt="Impaktemos"><img src="/logo universidad.png" alt="Universidad"><img src="/logo yprealty.png" alt="YP Realty"><img src="/logo eurobuilding.png" alt="Eurobuilding Hotels"><img src="/logo panhouse.png" alt="PanHouse">
        <img src="/logo plano creativo.png" alt="Plano Creativo"><img src="/logo dislocal.png" alt="Dislocal"><img src="/logo plano mariano moreno.png" alt="Instituto Superior Mariano Moreno"><img src="/logo impaktemos.png" alt="Impaktemos"><img src="/logo universidad.png" alt="Universidad"><img src="/logo yprealty.png" alt="YP Realty"><img src="/logo eurobuilding.png" alt="Eurobuilding Hotels"><img src="/logo panhouse.png" alt="PanHouse">
      </div>
    </div>
  </section>

  <!-- 2. ABOUT ME -->
  <section id="about" class="about-section">
    <div class="about-container">
      <div class="sticky-note reveal">
        <svg class="real-pin" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M12 10V22" stroke="#94a3b8" stroke-width="2" stroke-linecap="round"/><circle cx="12" cy="7" r="5" fill="#ef4444" stroke="#b91c1c" stroke-width="1.5"/><circle cx="10.5" cy="5.5" r="1.5" fill="#ffffff" opacity="0.6"/></svg>
        <span class="note-text" data-es="Sobre mí" data-en="About Me">Sobre mí</span>
      </div>
      <div class="about-header reveal">
        <h2 data-es="Sobre mí" data-en="About me">Sobre mí</h2>
        <p class="about-subtitle">Comunicadora social enfocada en tres áreas que considero inseparables para cualquier negocio que quiera vender y crecer en digital: desarrollo web front-end, marketing social media, ventas y lideración de proyectos.</p>
      </div>

      <div class="mac-window reveal">
        <div class="mac-titlebar">
          <div class="mac-buttons"><span class="mac-dot red"></span><span class="mac-dot yellow"></span><span class="mac-dot green"></span></div>
        </div>
        <div class="mac-content">
          <div class="bio-image-col"><img src="/memoji.png" alt="Memoji Greydemar" class="memoji-circle animated-memoji" /></div>
          <div class="bio-info-col">
            <p class="bio-intro" data-es="Soy <strong>Greydemar Serrano</strong>, comunicadora social, con más de 5 años de experiencia con una visión estratégica orientada a los resultados." data-en="I am <strong>Greydemar Serrano</strong>, a social communicator, with over 5 years of experience with a strategic, results-oriented vision.">Soy <strong>Greydemar Serrano</strong>, comunicadora social, con más de 5 años de experiencia con una visión estratégica orientada a los resultados.</p>
            <div class="stats-grid-about">
              <div class="stat-item"><h3 class="stat-number">+5</h3><p data-es="Años de experiencia" data-en="Years of experience">Años de experiencia</p></div>
              <div class="stat-item"><h3 class="stat-number">+8</h3><p data-es="Proyectos coordinados" data-en="Projects coordinated">Proyectos coordinados</p></div>
              <div class="stat-item"><h3 class="stat-number">+5</h3><p data-es="Eventos de alto impacto" data-en="High-impact events">Eventos de alto impacto</p></div>
              <div class="stat-item"><h3 class="stat-number">+13</h3><p data-es="Marcas y organizaciones" data-en="Brands and organizations">Marcas y organizaciones</p></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 3. PORTAFOLIO POR ÁREAS DE EXPERTISE -->
  <section id="projects" class="projects-section">
    <div class="projects-container">
      <div class="sticky-note reveal">
        <svg class="real-pin pin-blue" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M12 10V22" stroke="#94a3b8" stroke-width="2" stroke-linecap="round"/><circle cx="12" cy="7" r="5" fill="#3b82f6" stroke="#1d4ed8" stroke-width="1.5"/><circle cx="10.5" cy="5.5" r="1.5" fill="#ffffff" opacity="0.6"/></svg>
        <span class="note-text" data-es="Portafolio" data-en="Portfolio">Portafolio</span>
      </div>
      <div class="section-header reveal">
        <h2 data-es="Proyectos que cuentan historias." data-en="Projects that tell stories.">Proyectos que cuentan historias.</h2>
        <p data-es="Organizado por áreas de expertise: del código al cierre comercial." data-en="Organized by area of expertise: from code to closing the deal.">Organizado por áreas de expertise: del código al cierre comercial.</p>
      </div>

      <nav class="area-nav reveal" aria-label="Áreas de expertise">
        <a href="#frontend"><span class="num">01</span>Front-End</a>
        <a href="#social"><span class="num">02</span>Social Media</a>
        <a href="#pm"><span class="num">03</span><span data-es="Ventas / Project Manager" data-en="Sales / Project Manager">Ventas / Project Manager</span></a>
      </nav>

      <!-- ============ ÁREA 01 · FRONT-END ============ -->
      <article id="frontend" class="mac-window area-window area-accordion" data-area="frontend">
        <div class="mac-titlebar">
          <div class="mac-buttons"><span class="mac-dot red"></span><span class="mac-dot yellow"></span><span class="mac-dot green"></span></div>
          <div class="window-label">~/portafolio/front-end</div><span class="area-titlebar-plus" aria-hidden="true">+</span>
        </div>
        <div class="area-body">
          <div class="area-head reveal">
            <div>
              <h3 class="area-title">Front-End Developer</h3>
              <p class="area-desc">Aquí te comparto páginas web y landing pages que he realizado</p>
            </div>
          </div>

          <!-- STACK TECNOLÓGICO -->
          <div class="stack-block">
            <p class="stack-title reveal" data-es="Stack tecnológico" data-en="Tech stack">Stack tecnológico</p>
            <div class="stack-grid">
              <div class="stack-item reveal">
                <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 3l1.6 17.2L12 22l6.4-1.8L20 3H4z" fill="#E34F26"/><path d="M12 4.5v15.9l5-1.4 1.3-13.9L12 4.5z" fill="#EF652A"/><path d="M15.6 7.5H8.4l.3 3h6.1l-.35 4-2.45.8-2.45-.8-.15-1.8" stroke="#fff" stroke-width="1.3" fill="none" stroke-linecap="round" stroke-linejoin="round"/></svg>
                <span>HTML5</span>
              </div>
              <div class="stack-item reveal">
                <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 3l1.6 17.2L12 22l6.4-1.8L20 3H4z" fill="#1572B6"/><path d="M12 4.5v15.9l5-1.4 1.3-13.9L12 4.5z" fill="#33A9DC"/><path d="M15.6 7.5H8.4l.25 2.8h6.7l-.4 4.4-2.95.9-2.95-.9-.15-1.9" stroke="#fff" stroke-width="1.3" fill="none" stroke-linecap="round" stroke-linejoin="round"/></svg>
                <span>CSS3</span>
              </div>
              <div class="stack-item reveal">
                <svg viewBox="0 0 24 24" aria-hidden="true"><rect x="3" y="3" width="18" height="18" rx="3" fill="#F7DF1E"/><text x="12" y="17" font-size="10.5" font-weight="900" font-family="sans-serif" text-anchor="middle" fill="#0f172a">JS</text></svg>
                <span>Vanilla JavaScript</span>
              </div>
              <div class="stack-item reveal">
                <svg viewBox="0 0 24 24" aria-hidden="true"><path fill="#007ACC" fill-rule="evenodd" d="M17.2 2.3 9.6 9.4 5 5.9 3 7v10l2 1.1 4.6-3.5 7.6 7.1 3.8-1.8V4.1l-3.8-1.8zM17 8.1v7.8l-4.7-3.9L17 8.1z"/></svg>
                <span>VS Code</span>
              </div>
              <div class="stack-item reveal">
                <svg viewBox="0 0 24 24" fill="none" stroke="#0f172a" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 22v-4a4.8 4.8 0 0 0-1-3.5c3 0 6-2 6-5.5.08-1.25-.27-2.48-1-3.5.28-1.15.28-2.35 0-3.5 0 0-1 0-3 1.5-2.64-.5-5.36-.5-8 0C6 2 5 2 5 2c-.3 1.15-.3 2.35 0 3.5A5.403 5.403 0 0 0 4 9c0 3.5 3 5.5 6 5.5-.39.49-.68 1.05-.85 1.65-.17.6-.22 1.23-.15 1.85v4"/><path d="M9 18c-4.51 2-5-2-7-2"/></svg>
                <span>Git &amp; GitHub</span>
              </div>
              <div class="stack-item reveal">
                <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 3 22 20.5H2L12 3z" fill="#0f172a"/></svg>
                <span>Vercel</span>
              </div>
              <div class="stack-item reveal">
                <svg viewBox="0 0 24 24" fill="none" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="11" cy="11" r="7"/><line x1="21" y1="21" x2="16.5" y2="16.5"/><polyline points="7.5 13.5 10 11 12 12.5 14.5 8.5"/></svg>
                <span>SEO Basics</span>
              </div>
            </div>
          </div>
<div class="media-grid">
            <figure class="media-card bento-horizontal reveal">
              <video playsinline loop muted preload="metadata" class="horizontal-vid lazy-video" onclick="openGalleryZoom(this)">
                <source src="/laura navarro.mp4" type="video/mp4" />
              </video>
              <figcaption class="media-caption"><span class="cap-title">Diseño Web: Dra. Laura Navarro</span> <span class="cap-sep" aria-hidden="true">|</span> <span class="cap-company">Freelance</span></figcaption>
              <a href="https://gserrano2024cs-ctrl.github.io/Dra.LauraNavarroActiveLearningEducationLeadership/" target="_blank" rel="noopener" class="media-link">🌐 Ver sitio web</a>
            </figure>

            <figure class="media-card bento-horizontal reveal">
              <img src="/landingpage plataforma.png" alt="Diseño de Landing Page" class="horizontal-img w-full-img" onclick="openGalleryZoom(this)" />
              <figcaption class="media-caption"><span class="cap-title">Diseño UI/UX de Landing Page</span> <span class="cap-sep" aria-hidden="true">|</span> <span class="cap-company">Impaktemos</span></figcaption>
              <a href="https://minitas-mission-guide.lovable.app/" target="_blank" rel="noopener" class="media-link">🌐 Ver Landing Page</a>
            </figure>

            <figure class="media-card bento-horizontal reveal">
              <video playsinline loop muted preload="metadata" class="horizontal-vid lazy-video" onclick="openGalleryZoom(this)">
                <source src="/bea briceño.mp4" type="video/mp4" />
              </video>
              <figcaption class="media-caption"><span class="cap-title">Diseño Web: Beatriz Briceño</span> <span class="cap-sep" aria-hidden="true">|</span> <span class="cap-company">Freelance</span></figcaption>
              <a href="https://beatriz-brice-o-pic-n.vercel.app/" target="_blank" rel="noopener" class="media-link">🌐 Ver sitio web</a>
            </figure>

            <figure class="media-card bento-horizontal reveal">
              <video playsinline loop muted preload="metadata" class="horizontal-vid lazy-video" onclick="openGalleryZoom(this)">
                <source src="/mbi.mp4" type="video/mp4" />
              </video>
              <figcaption class="media-caption"><span class="cap-title">Diseño Web: Mario Briceño Iragorry</span> <span class="cap-sep" aria-hidden="true">|</span> <span class="cap-company">Freelance</span></figcaption>
              <a href="https://mbihoy.vercel.app/?v=1" target="_blank" rel="noopener" class="media-link">🌐 Ver sitio web</a>
            </figure>
          </div>
          <a href="#frontend-more" class="area-more-btn">Ver más</a>
        </div>
      </article>

      <!-- ============ ÁREA 02 · SOCIAL MEDIA ============ -->
      <article id="social" class="mac-window area-window area-accordion" data-area="social">
        <div class="mac-titlebar">
          <div class="mac-buttons"><span class="mac-dot red"></span><span class="mac-dot yellow"></span><span class="mac-dot green"></span></div>
          <div class="window-label">~/portafolio/social-media</div><span class="area-titlebar-plus" aria-hidden="true">+</span>
        </div>
        <div class="area-body">
          <div class="area-head reveal">
            <div>
              <h3 class="area-title">Social Media Marketing</h3>
              <p class="area-desc">Videos que he realizado para captar atención o en los que he participado para generar campañas de intriga, como la cobertura de comunicaciones de Interpanhouse (alianza con marca reconocida en Venezuela), tendencias, coberturas en festivales internacionales (Guadalajara), contenido educativo y cierres de ventas.</p>
            </div>
          </div>
<div class="stack-block social-stack-block">
            <p class="stack-title">Herramientas y plataformas</p>
            <div class="stack-grid">
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/youtube" alt="YouTube"><span>YouTube</span></div>
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/tiktok" alt="TikTok"><span>TikTok</span></div>
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/instagram" alt="Instagram"><span>Instagram</span></div>
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/facebook" alt="Facebook"><span>Facebook</span></div>
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/meta" alt="Meta"><span>Meta Business Suite</span></div>
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/zapier" alt="Zapier"><span>Zapier</span></div>
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/buffer" alt="Buffer"><span>Buffer</span></div>
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/googleanalytics" alt="Google Analytics"><span>Google Analytics 4</span></div>
            </div>
          </div>
          <div class="media-grid">
            <figure class="media-card bento-vertical reveal">
              <video playsinline loop muted preload="metadata" class="vertical-vid lazy-video" onclick="openGalleryZoom(this)">
                <source src="/VIDEO PARA PORTAFOLIO.webm" type="video/webm" />
              </video>
              <figcaption class="media-caption"><span class="cap-title">Cisterna de agua en PR</span></figcaption>
            </figure>
            <figure class="media-card bento-vertical reveal">
              <video playsinline loop muted preload="metadata" class="vertical-vid lazy-video" onclick="openGalleryZoom(this)">
                <source src="/PARA-PORTAFOLIO.webm" type="video/webm" />
              </video>
              <figcaption class="media-caption"><span class="cap-title">Cierre de venta en Arecibo, Puerto Rico</span></figcaption>
            </figure>
            <figure class="media-card bento-vertical reveal">
              <video playsinline loop muted preload="metadata" class="vertical-vid lazy-video" onclick="openGalleryZoom(this)">
                <source src="/VIDEO PARA PORTAFOLIO 2.webm" type="video/webm" />
              </video>
              <figcaption class="media-caption"><span class="cap-title">Trend: ¿quién paga qué en un arrendamiento?</span></figcaption>
            </figure>
            <figure class="media-card bento-vertical reveal">
              <video playsinline loop muted preload="metadata" class="vertical-vid lazy-video" onclick="openGalleryZoom(this)">
                <source src="/ISLA-DEL-ENCATO-PORTAFOLIO-.webm" type="video/webm" />
              </video>
              <figcaption class="media-caption"><span class="cap-title">Isla del Encanto</span></figcaption>
            </figure>
            <figure class="media-card bento-vertical reveal">
              <video playsinline loop muted preload="metadata" class="vertical-vid lazy-video" onclick="openGalleryZoom(this)">
                <source src="/video panhouse.mp4" type="video/mp4" />
              </video>
              <figcaption class="media-caption"><span class="cap-title">Cobertura de comunicaciones</span></figcaption>
            </figure>
            <figure class="media-card bento-vertical reveal">
              <video playsinline loop muted preload="metadata" class="vertical-vid lazy-video" onclick="openGalleryZoom(this)">
                <source src="/ph.mp4" type="video/mp4" />
              </video>
              <figcaption class="media-caption"><span class="cap-title">Feria Internacional del Libro en Guadalajara</span></figcaption>
            </figure>
          </div>
          <a href="#social-more" class="area-more-btn">Ver más</a>
        </div>
      </article>

      <!-- ============ ÁREA 03 · VENTAS / PROJECT MANAGER ============ -->
      <article id="pm" class="mac-window area-window area-accordion" data-area="pm">
        <div class="mac-titlebar">
          <div class="mac-buttons"><span class="mac-dot red"></span><span class="mac-dot yellow"></span><span class="mac-dot green"></span></div>
          <div class="window-label">~/portafolio/ventas-pm</div><span class="area-titlebar-plus" aria-hidden="true">+</span>
        </div>
        <div class="area-body">
          <div class="area-head reveal">
            <div>
              <h3 class="area-title" data-es="Ventas / Project Manager" data-en="Sales / Project Manager">Ventas / Project Manager</h3>
              <p class="area-desc" data-es="Coordinación operativa, logística de eventos y sistemas comerciales B2B bajo estándares exigentes. Convierto la complejidad de múltiples equipos en una sola operación sin fricción." data-en="Operational coordination, event logistics, and B2B commercial systems under demanding standards. I turn the complexity of multiple teams into a single frictionless operation.">Coordinación operativa, logística de eventos y sistemas comerciales B2B bajo estándares exigentes. Convierto la complejidad de múltiples equipos en una sola operación sin fricción.</p>
            </div>
          </div><div class="stack-block">
            <p class="stack-title">Herramientas y plataformas</p>
            <div class="stack-grid">
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/trello" alt="Trello"><span>Trello</span></div>
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/asana" alt="Asana"><span>Asana</span></div>
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/manychat" alt="ManyChat"><span>ManyChat</span></div>
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/googlesheets" alt="Google Sheets"><span>Google Sheets</span></div>
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/googledrive" alt="Google Drive"><span>Google Drive</span></div>
              <div class="stack-item"><img class="brand-logo-img" src="https://cdn.simpleicons.org/slack" alt="Slack"><span>Slack</span></div>
            </div>
          </div>
<div class="media-grid">
            <figure class="media-card bento-vertical reveal">
              <video playsinline loop muted preload="metadata" class="vertical-vid lazy-video" onclick="openGalleryZoom(this)">
                <source src="/video dislocal.mp4" type="video/mp4" />
              </video>
              <figcaption class="media-caption"><span class="cap-title">Proyecto en acción Dislocal</span><br><span class="cap-company">Cobertura de arquitectos en barrio El Calvario para infraestructura autosustentable.</span></figcaption>
            </figure>
            <figure class="media-card bento-vertical reveal">
              <video playsinline loop muted preload="metadata" class="vertical-vid lazy-video" onclick="openGalleryZoom(this)">
                <source src="/cerro verde ph.mp4" type="video/mp4" />
              </video>
              <figcaption class="media-caption"><span class="cap-title">Despliegue logístico en Cerro Verde</span><br><span class="cap-company">Festival del libro, stands y ventas.</span></figcaption>
            </figure>
            <figure class="media-card bento-vertical reveal">
              <video playsinline loop muted preload="metadata" class="vertical-vid lazy-video" onclick="openGalleryZoom(this)">
                <source src="/ciudad piar.mp4" type="video/mp4" />
              </video>
              <figcaption class="media-caption"><span class="cap-title">Intervención en los NPR</span></figcaption>
            </figure>
            <figure class="media-card bento-horizontal reveal">
              <img src="/dislocal 1.png" alt="Evento social de la comunidad en cine en el barrio El Calvario" class="horizontal-img w-full-img" onclick="openGalleryZoom(this)" />
              <figcaption class="media-caption"><span class="cap-title">Evento social de la comunidad en cine en el barrio El Calvario</span><br><span class="cap-company">Sondeo.</span></figcaption>
            </figure>
            <figure class="media-card bento-horizontal reveal">
              <img src="/evento ph.png" alt="Lanzamiento editorial - Coordinación de lanzamiento" class="horizontal-img w-full-img" onclick="openGalleryZoom(this)" />
              <figcaption class="media-caption"><span class="cap-title">Lanzamiento editorial - Coordinación de lanzamiento</span></figcaption>
            </figure>
          </div>
        </div>
      </article>
    </div>
  </section>

  <!-- 4. SERVICIOS · PROBLEMA → SOLUCIÓN -->
  <section id="services" class="services-section">
    <div class="services-container">
      <div class="sticky-note reveal">
        <svg class="real-pin" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M12 10V22" stroke="#94a3b8" stroke-width="2" stroke-linecap="round"/><circle cx="12" cy="7" r="5" fill="#f59e0b" stroke="#b45309" stroke-width="1.5"/><circle cx="10.5" cy="5.5" r="1.5" fill="#ffffff" opacity="0.6"/></svg>
        <span class="note-text" data-es="Servicios" data-en="Services">Servicios</span>
      </div>
      <div class="section-header reveal">
        <h2 data-es="Servicios" data-en="Services">Servicios</h2>
        <p>Soluciones concretas para resolver problemas concretos.</p>
      </div>

      <div class="services-grid-blocks">
        <div class="service-block reveal" data-service="Desarrollo Front-End & Landing Pages" style="background: rgba(16, 185, 129, 0.08); border-color: rgba(16, 185, 129, 0.2);">
          <div class="service-block-header"><h3>Desarrollo Front-End & Landing Pages</h3><span class="icon-plus" aria-hidden="true">+</span></div>
          <div class="service-block-body"><div class="service-block-inner">
            <div class="service-copy"><span class="flow-label">Problema</span><p>Tu negocio no tiene una casa digital que convierta.</p><span class="flow-label">Solución</span><p>Te creo un espacio para tus ventas vinculado con automatizaciones de pagos.</p></div>
            <a class="service-whatsapp" target="_blank" rel="noopener" href="https://wa.me/584126339776?text=Hola%20Grey,%20vi%20tu%20portafolio,%20me%20interesa%20el%20servicio%20de%20Desarrollo%20Front-End%20%26%20Landing%20Pages">Solicitar servicio</a>
          </div></div>
        </div>
        <div class="service-block reveal" data-service="Social Media Marketing" style="background: rgba(59, 130, 246, 0.08); border-color: rgba(59, 130, 246, 0.2);">
          <div class="service-block-header"><h3>Social Media Marketing</h3><span class="icon-plus" aria-hidden="true">+</span></div>
          <div class="service-block-body"><div class="service-block-inner">
            <div class="service-copy"><span class="flow-label">Problema</span><p>Publicas, pero el contenido no está llevando a ventas.</p><span class="flow-label">Solución</span><p>Convierto ideas en contenido estratégico, medible y orientado a conversión.</p></div>
            <a class="service-whatsapp" target="_blank" rel="noopener" href="https://wa.me/584126339776?text=Hola%20Grey,%20vi%20tu%20portafolio,%20me%20interesa%20el%20servicio%20de%20Social%20Media%20Marketing">Solicitar servicio</a>
          </div></div>
        </div>
        <div class="service-block reveal" data-service="Edición de Contenido Visual" style="background: rgba(168, 85, 247, 0.08); border-color: rgba(168, 85, 247, 0.2);">
          <div class="service-block-header"><h3>Edición de Contenido Visual</h3><span class="icon-plus" aria-hidden="true">+</span></div>
          <div class="service-block-body"><div class="service-block-inner">
            <div class="service-copy"><span class="flow-label">Problema</span><p>Tienes material, pero no logra detener el scroll.</p><span class="flow-label">Solución</span><p>Edición con hooks, ritmo, subtítulos y formatos pensados para retención.</p></div>
            <a class="service-whatsapp" target="_blank" rel="noopener" href="https://wa.me/584126339776?text=Hola%20Grey,%20vi%20tu%20portafolio,%20me%20interesa%20el%20servicio%20de%20Edición%20de%20Contenido%20Visual">Solicitar servicio</a>
          </div></div>
        </div>
        <div class="service-block reveal" data-service="Ventas, CRM & Prospección" style="background: rgba(245, 158, 11, 0.08); border-color: rgba(245, 158, 11, 0.2);">
          <div class="service-block-header"><h3>Ventas, CRM & Prospección</h3><span class="icon-plus" aria-hidden="true">+</span></div>
          <div class="service-block-body"><div class="service-block-inner">
            <div class="service-copy"><span class="flow-label">Problema</span><p>Los leads llegan y se enfrían por falta de seguimiento.</p><span class="flow-label">Solución</span><p>Ordeno tu proceso comercial y sus puntos de seguimiento para facilitar el cierre.</p></div>
            <a class="service-whatsapp" target="_blank" rel="noopener" href="https://wa.me/584126339776?text=Hola%20Grey,%20vi%20tu%20portafolio,%20me%20interesa%20el%20servicio%20de%20Ventas,%20CRM%20%26%20Prospección">Solicitar servicio</a>
          </div></div>
        </div>
        <div class="service-block reveal" data-service="Project Management" style="background: rgba(100, 116, 139, 0.08); border-color: rgba(100, 116, 139, 0.2);">
          <div class="service-block-header"><h3>Project Management</h3><span class="icon-plus" aria-hidden="true">+</span></div>
          <div class="service-block-body"><div class="service-block-inner">
            <div class="service-copy"><span class="flow-label">Problema</span><p>Tu proyecto tiene tareas, personas y fechas dispersas.</p><span class="flow-label">Solución</span><p>Organizo prioridades, responsables y entregables para que la ejecución avance.</p></div>
            <a class="service-whatsapp" target="_blank" rel="noopener" href="https://wa.me/584126339776?text=Hola%20Grey,%20vi%20tu%20portafolio,%20me%20interesa%20el%20servicio%20de%20Project%20Management">Solicitar servicio</a>
          </div></div>
        </div>
      </div>
    </div>
  </section>

  <!-- 5. CONTACTO -->
  <section id="contact" class="contact-section">
    <div class="contact-container mac-window reveal">
      <div class="contact-left light-gray-bg">
        <h2 class="coffee-title">
          <span data-es="Tomemos un café virtual." data-en="Let's have a virtual coffee.">Tomemos un café virtual.</span>
          <svg width="56" height="56" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="coffee-cup-svg">
            <path d="M17 8h1a4 4 0 1 1 0 8h-1" />
            <path d="M3 8h14v9a4 4 0 0 1-4 4H7a4 4 0 0 1-4-4Z" />
            <path class="steam steam-1" d="M6 2c0 2-2 2-2 4s2 2 2 4" />
            <path class="steam steam-2" d="M10 2c0 2-2 2-2 4s2 2 2 4" />
            <path class="steam steam-3" d="M14 2c0 2-2 2-2 4s2 2 2 4" />
          </svg>
        </h2>
        <p data-es="Si buscas resultados, orden y alguien que entienda el juego digital de principio a fin, escríbeme y hagamos que las cosas pasen." data-en="If you are looking for results, order, and someone who understands the digital game from start to finish, write me and let's make things happen.">Si buscas resultados, orden y alguien que entienda el juego digital de principio a fin, escríbeme y hagamos que las cosas pasen.</p>
         <a class="coffee-cta" target="_blank" rel="noopener" href="https://wa.me/584126339776?text=Hola%20Grey,%20tomemos%20ese%20caf%C3%A9%20virtual">Tomemos ese café virtual</a>

        <div class="contact-socials-row">
          <a href="https://wa.me/584126339776" target="_blank" rel="noopener" class="social-icon-btn" title="WhatsApp" aria-label="WhatsApp"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"></path></svg></a>
          <a href="mailto:greydemar06@gmail.com" class="social-icon-btn" title="Correo" aria-label="Correo"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path><polyline points="22,6 12,13 2,6"></polyline></svg></a>
          <a href="https://www.linkedin.com/in/greydemarserrano/" target="_blank" rel="noopener" class="social-icon-btn" title="LinkedIn" aria-label="LinkedIn"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"></path><rect x="2" y="9" width="4" height="12"></rect><circle cx="4" cy="4" r="2"></circle></svg></a>
        </div>
      </div>

      <div class="contact-right glass-panel">
        <h3 style="margin-bottom: 24px; font-size: 1.8rem; font-weight: 800;" data-es="Déjame un mensaje." data-en="Leave me a message.">Déjame un mensaje.</h3>
        <form class="contact-form" id="wa-form" onsubmit="sendToWhatsApp(event)">
          <div class="input-group"><label for="wa-name" data-es="Tu Nombre" data-en="Your Name">Tu Nombre</label><input type="text" id="wa-name" required placeholder="Ej. Ana Pérez" /></div>
          <div class="input-group"><label for="wa-email" data-es="Tu Correo" data-en="Your Email">Tu Correo</label><input type="email" id="wa-email" required placeholder="ana@empresa.com" /></div>
          <div class="input-group"><label for="wa-message" data-es="Mensaje" data-en="Message">Mensaje</label><textarea id="wa-message" rows="4" required placeholder="Hola Grey, me gustaría que trabajáramos en..." data-es-placeholder="Hola Grey, me gustaría que trabajáramos en..."></textarea></div>

          <div class="privacy-disclaimer">
            <p data-es="Tus datos serán tratados por Greydemar Serrano con la finalidad de responder tu consulta. Puedes ejercer tus derechos de acceso, rectificación, cancelación y oposición en greydemar06@gmail.com. <a href='/terminos' target='_blank' class='privacy-link'>Más información</a>" data-en="Your data will be processed by Greydemar Serrano to answer your query. You may exercise your rights of access, rectification, cancellation, and objection at greydemar06@gmail.com. <a href='/terminos' target='_blank' class='privacy-link'>More info</a>">
              Tus datos serán tratados por Greydemar Serrano con la finalidad de responder tu consulta. Puedes ejercer tus derechos de acceso, rectificación, cancelación y oposición en greydemar06@gmail.com. <a href="/terminos" target="_blank" class="privacy-link">Más información</a>
            </p>
          </div>

          <div class="privacy-checkbox-group">
            <input type="checkbox" id="privacy-accept" required />
            <label for="privacy-accept" data-es="He leído y acepto la <a href='/terminos' target='_blank' class='privacy-link'>política de privacidad</a>" data-en="I have read and accept the <a href='/terminos' target='_blank' class='privacy-link'>privacy policy</a>">He leído y acepto la <a href="/terminos" target="_blank" class="privacy-link">política de privacidad</a></label>
          </div>

          <button type="submit" class="submit-btn" data-es="Enviar mensaje" data-en="Send message">Enviar mensaje</button>
        </form>
      </div>
    </div>
  </section>

  <!-- FOOTER NEGRO -->
  <footer class="site-footer black-footer">

    <!-- Easter Egg: Recordatorio (Lucas 24, 25-33) -->
    <div class="hidden-eyes-container" title="Lucas 24:31">
      <div style="display: flex; flex-direction: column; align-items: center;">
        <svg width="60" height="25" viewBox="0 0 100 40" fill="none" xmlns="http://www.w3.org/2000/svg">
          <g class="blinking-eyes">
            <!-- Pestañas Izquierda -->
            <path d="M15,10 L10,0 M25,8 L25,-2 M35,10 L40,0" stroke="#ffffff" stroke-width="1.5"/>
            <!-- Ojo Izquierdo -->
            <path d="M10,20 Q25,5 40,20 Q25,35 10,20 Z" stroke="#ffffff" stroke-width="2.5" fill="none"/>
            <circle cx="25" cy="20" r="4.5" fill="#ffffff"/>

            <!-- Pestañas Derecha -->
            <path d="M65,10 L60,0 M75,8 L75,-2 M85,10 L90,0" stroke="#ffffff" stroke-width="1.5"/>
            <!-- Ojo Derecho -->
            <path d="M60,20 Q75,5 90,20 Q75,35 60,20 Z" stroke="#ffffff" stroke-width="2.5" fill="none"/>
            <circle cx="75" cy="20" r="4.5" fill="#ffffff"/>
          </g>
        </svg>
        <span style="font-size: 9px; letter-spacing: 2px; color: #ffffff; margin-top: 6px; font-weight: 700;">LUCAS 24, 25-33</span>
      </div>
    </div>

    <p data-es="© 2026 Greydemar Serrano. Todos los derechos reservados." data-en="© 2026 Greydemar Serrano. All rights reserved.">© 2026 Greydemar Serrano. Todos los derechos reservados.</p>
  </footer>

  <!-- DOCK FLOTANTE · LIQUID GLASS -->
  <nav class="floating-dock" aria-label="Navegación principal">
    <ul class="dock-list">
      <li class="dock-item" data-target="about" data-tooltip="Sobre mí" data-tooltip-es="Sobre mí" data-tooltip-en="About Me"><a href="#about" class="dock-link"><img src="/notas.webp" class="mac-icon" alt="Notas"></a></li>
      <li class="dock-item" data-target="projects" data-tooltip="Portafolio" data-tooltip-es="Portafolio" data-tooltip-en="Portfolio"><a href="#projects" class="dock-link"><img src="/foto.png" class="mac-icon" alt="Fotos"></a></li>
      <li class="dock-item" data-target="services" data-tooltip="Servicios" data-tooltip-es="Servicios" data-tooltip-en="Services"><a href="#services" class="dock-link"><img src="/finder icon.png" class="mac-icon" alt="Finder"></a></li>
      <li class="dock-item" data-target="contact" data-tooltip="Contacto" data-tooltip-es="Contacto" data-tooltip-en="Contact"><a href="#contact" class="dock-link"><img src="/ICON MAIL.webp" class="mac-icon" alt="Mail"></a></li>
    </ul>
  </nav>

  <script is:inline>
    /* ---------- IDIOMA ---------- */
    function setLanguage(lang) {
      document.documentElement.lang = lang;

      document.querySelectorAll('.lang-btn').forEach(function (btn) {
        btn.classList.toggle('active', btn.getAttribute('data-lang') === lang);
      });

      document.querySelectorAll('[data-es]').forEach(function (el) {
        var value = el.getAttribute('data-' + lang);
        if (value !== null) { el.innerHTML = value; }
      });

      document.querySelectorAll('[data-tooltip-es]').forEach(function (el) {
        el.setAttribute('data-tooltip', el.getAttribute('data-tooltip-' + lang));
      });

      var msgArea = document.getElementById('wa-message');
      if (msgArea) {
        msgArea.placeholder = lang === 'es' ? msgArea.getAttribute('data-es-placeholder') : "Hi Grey, I'd like to work on...";
      }
    }

    /* ---------- ZOOM DE MEDIA ---------- */
    function openGalleryZoom(element) {
      var overlay = document.getElementById('zoom-overlay');
      var container = document.getElementById('zoom-media-container');
      var caption = document.getElementById('zoom-caption');

      container.innerHTML = '';

      if (element.tagName === 'VIDEO') {
        var video = document.createElement('video');
        video.src = element.currentSrc || element.querySelector('source').src;
        video.controls = true;
        video.autoplay = true;
        video.playsInline = true;
        video.className = 'zoom-media';
        container.appendChild(video);
      } else {
        var img = document.createElement('img');
        img.src = element.currentSrc || element.src;
        img.alt = element.alt || '';
        img.className = 'zoom-media';
        container.appendChild(img);
      }

      /* El pie del zoom se toma del caption visible (siempre en el idioma activo) */
      var card = element.closest('.media-card');
      var cap = card ? card.querySelector('.media-caption') : null;
      var text = cap ? cap.textContent.replace(/\s+/g, ' ').trim() : (element.getAttribute('data-caption') || '');
      caption.textContent = text;

      overlay.classList.add('active');
    }

    function openProfileZoom(event) {
      event.stopPropagation();
      openGalleryZoom(event.currentTarget);
    }

    function closeZoomNow() {
      document.getElementById('zoom-overlay').classList.remove('active');
      document.getElementById('zoom-media-container').innerHTML = '';
    }

    function closeZoom(e) {
      if (e.target.id === 'zoom-overlay' || e.target.classList.contains('zoom-close')) { closeZoomNow(); }
    }

    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape') { closeZoomNow(); }
    });

    /* ---------- FORMULARIO → WHATSAPP ---------- */
    function sendToWhatsApp(event) {
      event.preventDefault();
      var acceptCheckbox = document.getElementById('privacy-accept');
      if (!acceptCheckbox.checked) {
        alert("Debes aceptar la política de privacidad para enviar el mensaje.");
        return;
      }
      var name = document.getElementById('wa-name').value;
      var email = document.getElementById('wa-email').value;
      var message = document.getElementById('wa-message').value;
      var phone = "584126339776";
      var text = "¡Hola Grey! Soy " + name + " (" + email + ").\n\n" + message;
      window.open("https://wa.me/" + phone + "?text=" + encodeURIComponent(text), '_blank');
    }

    /* ---------- TARJETAS DESPLEGABLES (hero + servicios) ---------- */
    document.querySelectorAll('[data-toggle]').forEach(function (el) {
      var cls = el.getAttribute('data-toggle');
      el.setAttribute('role', 'button');
      el.setAttribute('tabindex', '0');
      el.setAttribute('aria-expanded', String(el.classList.contains(cls)));

      function toggle() {
        var open = el.classList.toggle(cls);
        el.setAttribute('aria-expanded', String(open));
      }
      el.addEventListener('click', toggle);
      el.addEventListener('keydown', function (e) {
        if ((e.key === 'Enter' || e.key === ' ') && e.target === el) {
          e.preventDefault();
          toggle();
        }
      });
    });

    /* ---------- ACORDEONES DE ÁREAS ---------- */
    (function () {
      var buttons = document.querySelectorAll('[data-area-target]');
      var areas = document.querySelectorAll('.area-accordion');

      function setArea(id, open) {
        var area = document.querySelector('[data-area="' + id + '"]');
        var button = document.querySelector('[data-area-target="' + id + '"]');
        if (!area || !button) return;
        area.classList.toggle('is-open', open);
        button.setAttribute('aria-expanded', String(open));
      }

      buttons.forEach(function (button) {
        button.addEventListener('click', function () {
          var id = button.getAttribute('data-area-target');
          var area = document.querySelector('[data-area="' + id + '"]');
          var willOpen = !area.classList.contains('is-open');
          areas.forEach(function (item) { item.classList.remove('is-open'); });
          buttons.forEach(function (item) { item.setAttribute('aria-expanded', 'false'); });
          setArea(id, willOpen);
        });
      });

      areas.forEach(function (area) {
        var bar = area.querySelector('.mac-titlebar');
        if (!bar) return;
        bar.addEventListener('click', function (event) {
          if (event.target.closest('a, button')) return;
          var id = area.getAttribute('data-area');
          var button = document.querySelector('[data-area-target="' + id + '"]');
          if (button) button.click();
        });
      });
    })();

    /* ---------- SERVICIOS: todos cerrados por defecto ---------- */
    (function () {
      document.querySelectorAll('.service-block').forEach(function (block) {
        block.classList.remove('open');
        var header = block.querySelector('.service-block-header');
        if (header) {
          header.setAttribute('role', 'button');
          header.setAttribute('tabindex', '0');
          header.setAttribute('aria-expanded', 'false');
          function toggleService() {
            var open = block.classList.toggle('open');
            header.setAttribute('aria-expanded', String(open));
          }
          header.addEventListener('click', toggleService);
          header.addEventListener('keydown', function (e) {
            if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); toggleService(); }
          });
        }
      });
    })();

    /* ---------- CAFÉ: LLENADO + HUMO SEGÚN SCROLL ---------- */
    (function () {
      var section = document.getElementById('contact');
      var liquid = document.querySelector('.coffee-liquid');
      if (!section || !liquid) return;

      function updateCoffee() {
        var rect = section.getBoundingClientRect();
        var viewport = window.innerHeight;
        var start = viewport * 0.95;
        var end = viewport * 0.25;
        var progress = (start - rect.top) / (start - end);
        progress = Math.max(0, Math.min(1, progress));
        liquid.style.transform = 'scaleY(' + progress + ')';
        section.classList.toggle('coffee-section-active', progress > 0.08);
        liquid.style.opacity = String(Math.max(0.25, progress));
      }

      updateCoffee();
      window.addEventListener('scroll', updateCoffee, { passive: true });
      window.addEventListener('resize', updateCoffee);
    })();

    /* ---------- ANIMACIONES DE ENTRADA AL HACER SCROLL ---------- */
    (function () {
      var items = document.querySelectorAll('.reveal');
      if (!('IntersectionObserver' in window)) {
        items.forEach(function (el) { el.classList.add('in-view'); });
        return;
      }
      var io = new IntersectionObserver(function (entries) {
        var visible = entries.filter(function (e) { return e.isIntersecting; });
        visible.forEach(function (entry, i) {
          /* Escalonado suave entre los elementos que entran a la vez */
          entry.target.style.setProperty('--d', Math.min(i, 5));
          entry.target.classList.add('in-view');
          io.unobserve(entry.target);
        });
      }, { threshold: 0.12, rootMargin: '0px 0px -6% 0px' });
      items.forEach(function (el) { io.observe(el); });
    })();

    /* ---------- VIDEOS: reproducir solo lo visible (rendimiento) ---------- */
    (function () {
      var videos = document.querySelectorAll('video.lazy-video');
      if (!('IntersectionObserver' in window)) {
        videos.forEach(function (v) { v.setAttribute('autoplay', ''); });
        return;
      }
      var vio = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          var v = entry.target;
          if (entry.isIntersecting) {
            var p = v.play();
            if (p && p.catch) { p.catch(function () {}); }
          } else {
            v.pause();
          }
        });
      }, { threshold: 0.25 });
      videos.forEach(function (v) { vio.observe(v); });
    })();

    /* ---------- DOCK: sección activa ---------- */
    (function () {
      if (!('IntersectionObserver' in window)) { return; }
      var dockItems = document.querySelectorAll('.dock-item');
      var spy = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (entry.isIntersecting) {
            dockItems.forEach(function (item) {
              item.classList.toggle('active', item.getAttribute('data-target') === entry.target.id);
            });
          }
        });
      }, { rootMargin: '-45% 0px -50% 0px' });
      ['about', 'projects', 'services', 'contact'].forEach(function (id) {
        var section = document.getElementById(id);
        if (section) { spy.observe(section); }
      });
    })();

    document.addEventListener('DOMContentLoaded', function () {
      document.body.classList.add('loaded');
    });
  </script>

</body>
</html>

<style is:global>
  :root {
    --navy-dark: #0f172a; --pastel-yellow: #fef08a; --pastel-pink: #cbd5e1; --pastel-green: #64748b;
    --glass-bg: rgba(255, 255, 255, 0.4); --glass-border: rgba(255, 255, 255, 0.6); --glass-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.08);
    --text-muted: #475569; --bg-soft: rgba(255, 255, 255, 0.7); --border-color: rgba(0, 0, 0, 0.1);
    --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
    --ease-ios: cubic-bezier(0.32, 0.72, 0, 1);
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  /* SMOOTH SCROLL GLOBAL (debe ir en html, no en body) */
  html { scroll-behavior: smooth; scroll-padding-top: 24px; }
  body { font-family: 'Plus Jakarta Sans', sans-serif; color: var(--navy-dark); overflow-x: hidden; position: relative; }

  a:focus-visible, button:focus-visible, [role="button"]:focus-visible { outline: 2px solid #3b82f6; outline-offset: 3px; }

  /* ---------- CARGA DEL HERO ---------- */
  .js .fade-in-section { opacity: 0; transform: translateY(30px); transition: opacity 0.8s ease, transform 0.8s var(--ease-out); }
  .js body.loaded .fade-in-section { opacity: 1; transform: translateY(0); }

  /* ---------- ENTRADA AL HACER SCROLL ---------- */
  .js .reveal { opacity: 0; }
  .js .reveal.in-view { opacity: 1; animation: reveal-up 0.85s var(--ease-out) backwards; animation-delay: calc(var(--d, 0) * 90ms); }
  @keyframes reveal-up { from { opacity: 0; transform: translateY(28px); } to { opacity: 1; transform: translateY(0); } }

  @media (prefers-reduced-motion: reduce) {
    html { scroll-behavior: auto; }
    .js .reveal { opacity: 1; }
    .js .reveal.in-view { animation: none; }
    .js .fade-in-section { transition: none; }
    .marquee-track, .blinking-eyes { animation: none; }
  }

  .global-fixed-bg { position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background-color: #f8fafc; background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.05'/%3E%3C/svg%3E"); z-index: -999; }
  .sticker { position: absolute; opacity: 0.35; filter: grayscale(1); }
  .sticker-star { top: 12%; right: 15%; transform: rotate(15deg); }
  .sticker-plant { bottom: 20%; left: 8%; transform: rotate(-10deg); opacity: 0.6; }
  .sticker-sparkle { top: 40%; right: 5%; transform: rotate(5deg); }

  /* ---------- ZOOM ---------- */
  .zoom-overlay { position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(0,0,0,0.92); z-index: 999999; display: flex; flex-direction: column; justify-content: center; align-items: center; opacity: 0; visibility: hidden; transition: all 0.3s ease; padding: 20px; backdrop-filter: blur(5px); }
  .zoom-overlay.active { opacity: 1; visibility: visible; }
  .zoom-content-wrapper { position: relative; display: flex; flex-direction: column; align-items: center; max-width: 90vw; }
  .zoom-media { max-width: 100%; max-height: 75vh; border-radius: 12px; box-shadow: 0 10px 40px rgba(0,0,0,0.5); object-fit: contain; }
  .zoom-caption { color: #ffffff; margin-top: 16px; font-size: 1.15rem; font-weight: 600; text-align: center; max-width: 600px; text-shadow: 0 2px 4px rgba(0,0,0,0.8); }
  .zoom-close { position: absolute; top: 25px; right: 35px; background: rgba(255,255,255,0.15); border: none; color: white; width: 44px; height: 44px; border-radius: 50%; font-size: 22px; cursor: pointer; transition: background 0.3s; z-index: 9999999; display: flex; align-items: center; justify-content: center; }
  .zoom-close:hover { background: rgba(255,255,255,0.35); }

  /* ---------- HERO ---------- */
  .hero-wrapper { position: relative; width: 100%; min-height: 100vh; overflow: hidden; z-index: 10; background: #000; }
  .hero-video-container { position: absolute; top: 0; left: 0; width: 100%; height: 100%; z-index: -1; }
  .video-bg { width: 100%; height: 100%; object-fit: cover; opacity: 0.8; }
  .avila-overlay { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.35); }
  .top-header { position: relative; width: 100%; max-width: 1300px; margin: 0 auto; padding: 24px 32px; display: flex; justify-content: flex-end; align-items: center; z-index: 10; }
  .brand-centered { position: absolute; top: 38px; left: 50%; transform: translateX(-50%); font-weight: 900; font-size: 20px; letter-spacing: -0.02em; color: #ffffff; text-shadow: 0 2px 10px rgba(0,0,0,0.4); white-space: nowrap; }

  .lang-switch.dark-mode { background: rgba(0, 0, 0, 0.6); backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px); padding: 4px; border-radius: 30px; display: flex; align-items: center; gap: 4px; border: 1px solid rgba(255,255,255,0.1); }
  .lang-btn { background: transparent; border: none; font-weight: 700; font-size: 12px; color: rgba(255,255,255,0.6); cursor: pointer; padding: 6px 12px; border-radius: 20px; transition: all 0.3s ease; }
  .lang-btn.active { background: rgba(255,255,255,0.15); color: #ffffff; font-weight: 900; }

  /* Grid: la columna izquierda se estira para que la frase quede abajo sin superponerse al card */
  .hero-container { max-width: 1300px; width: 100%; margin: 0 auto; padding: 20px 32px 110px; display: grid; grid-template-columns: 320px 1fr 260px; gap: 24px; align-items: center; min-height: calc(100vh - 100px); position: relative; z-index: 10; }
  .left-col { display: flex; flex-direction: column; justify-content: center; gap: 32px; }

  /* ---------- LIQUID GLASS (estilo iOS "Clear") ---------- */
  .liquid-glass {
    position: relative; isolation: isolate;
    background:
      linear-gradient(135deg, rgba(255,255,255,0.20) 0%, rgba(255,255,255,0.05) 42%, rgba(255,255,255,0.12) 100%),
      rgba(10, 16, 30, 0.40);
    -webkit-backdrop-filter: blur(16px) saturate(180%) brightness(1.08);
    backdrop-filter: blur(16px) saturate(180%) brightness(1.08);
    border: 1px solid rgba(255,255,255,0.22);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.55),
      inset 0 -1px 0 rgba(255,255,255,0.10),
      inset 1px 0 0 rgba(255,255,255,0.14),
      inset 0 0 26px rgba(255,255,255,0.08),
      0 12px 32px rgba(0,0,0,0.30),
      0 2px 6px rgba(0,0,0,0.18);
    color: #ffffff;
    transition: transform 0.5s var(--ease-ios), box-shadow 0.5s var(--ease-ios), background 0.5s ease;
  }
  /* Reflejo especular en el borde */
  .liquid-glass::before { content: ""; position: absolute; inset: 0; border-radius: inherit; padding: 1px; z-index: -1; pointer-events: none;
    background: linear-gradient(145deg, rgba(255,255,255,0.85) 0%, rgba(255,255,255,0) 28%, rgba(255,255,255,0) 68%, rgba(255,255,255,0.45) 100%);
    -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
    -webkit-mask-composite: xor; mask-composite: exclude; }
  /* Brillo superior curvo */
  .liquid-glass::after { content: ""; position: absolute; top: 0; left: 0; right: 0; height: 55%; border-radius: inherit; z-index: -1; pointer-events: none;
    background: radial-gradient(120% 100% at 15% 0%, rgba(255,255,255,0.22), rgba(255,255,255,0) 65%); }
  .liquid-glass:hover { transform: translateY(-2px); box-shadow: inset 0 1px 0 rgba(255,255,255,0.65), inset 0 -1px 0 rgba(255,255,255,0.14), inset 1px 0 0 rgba(255,255,255,0.18), inset 0 0 30px rgba(255,255,255,0.10), 0 18px 40px rgba(0,0,0,0.34), 0 2px 8px rgba(0,0,0,0.2); }

  /* Card perfil */
  .glass-profile-card { border-radius: 26px; padding: 16px 20px; cursor: pointer; overflow: hidden; }
  .card-min-header { display: flex; align-items: center; gap: 14px; flex-direction: row; }
  .shift-right { margin-left: 6px; }
  .profile-thumb { width: 48px; height: 48px; border-radius: 50%; object-fit: cover; border: 2px solid rgba(255, 255, 255, 0.9); transition: width 0.5s var(--ease-ios), height 0.5s var(--ease-ios), margin 0.5s var(--ease-ios), box-shadow 0.4s ease; flex-shrink: 0; cursor: zoom-in; }
  .glass-profile-card.active .profile-thumb { width: 72px; height: 72px; border-width: 3px; box-shadow: 0 4px 14px rgba(0,0,0,0.3); margin-left: 0; }

  .min-meta { display: flex; flex-direction: column; justify-content: center; min-width: 0; }
  .status-line { display: flex; align-items: center; gap: 6px; margin-bottom: 4px; }
  @keyframes blink-green { 0% { opacity: 1; } 50% { opacity: 0.3; } 100% { opacity: 1; } }
  .green-dot { width: 8px; height: 8px; background: #cbd5e1; border-radius: 50%; border: 1px solid rgba(255,255,255,0.8); }
  .status-text { font-size: 11px; font-weight: 700; color: #ffffff; text-shadow: 0 1px 2px rgba(0,0,0,0.35); }
  .name-group { display: flex; flex-direction: column; gap: 2px; }
  .min-name { font-size: 15px; font-weight: 900; letter-spacing: -0.01em; line-height: 1.1; color: #ffffff; text-shadow: 0 1px 3px rgba(0,0,0,0.4); }
  .min-title { font-size: 11.5px; font-weight: 700; color: rgba(255,255,255,0.88); text-shadow: 0 1px 2px rgba(0,0,0,0.35); }

  .card-chevron { margin-left: auto; flex-shrink: 0; color: rgba(255,255,255,0.85); transition: transform 0.5s var(--ease-ios); }
  .glass-profile-card.active .card-chevron { transform: rotate(180deg); }

  /* Despliegue: anima la altura real (0fr → 1fr), sin saltos ni max-height arbitrarios */
  .card-expand { display: grid; grid-template-rows: 0fr; opacity: 0; transition: grid-template-rows 0.6s var(--ease-ios), opacity 0.4s ease; }
  .card-expand-inner { overflow: hidden; min-height: 0; }
  .card-expand-body { margin-top: 16px; padding-top: 16px; border-top: 1px solid rgba(255, 255, 255, 0.25); }
  .active > .card-expand { grid-template-rows: 1fr; opacity: 1; transition-delay: 0s, 0.1s; }

  .expand-bio { font-size: 14.5px; font-weight: 500; line-height: 1.55; margin-bottom: 16px; color: rgba(255,255,255,0.95); text-shadow: 0 1px 2px rgba(0,0,0,0.3); }
  .expand-bio strong { font-weight: 900; color: #ffffff; }

  .skills-title { font-size: 11px; font-weight: 800; color: #ffffff; text-transform: uppercase; margin-bottom: 8px; opacity: 0.9; }
  .skills-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 8px; }
  .skills-grid span { font-size: 10.5px; font-weight: 800; color: #ffffff; background: rgba(255, 255, 255, 0.16); border: 1px solid rgba(255,255,255,0.22); padding: 8px 10px; border-radius: 8px; text-align: center; white-space: nowrap; }
  .skills-grid .wide-skill { grid-column: span 2; }

  /* Frase del fondo del hero */
  .hero-quote-corner { max-width: 320px; }
  .hero-quote-corner span { font-size: 17px; font-weight: 800; letter-spacing: -0.02em; color: #ffffff; text-shadow: 0 2px 14px rgba(0,0,0,0.8); line-height: 1.4; display: block; }

  .hero-center-text { text-align: center; position: relative; display: flex; justify-content: center; align-items: center; align-self: center; }
  .hero-center-text h1 { font-size: 5.5rem; font-weight: 900; line-height: 0.98; letter-spacing: -0.04em; text-transform: uppercase; margin: 0 auto; max-width: 900px; color: #ffffff; text-shadow: 0 8px 30px rgba(0,0,0,0.3); }
  .floating-pill { position: absolute; padding: 6px 14px; border-radius: 30px; font-size: 12px; font-weight: 800; white-space: nowrap; color: var(--navy-dark); pointer-events: none; }
  .pill-pink { background: var(--pastel-pink); top: -25px; right: 15px; transform: rotate(3deg); }
  .pill-green { background: var(--pastel-green); bottom: -30px; left: 10%; transform: rotate(-6deg); }
  .pill-yellow { background: var(--pastel-yellow); bottom: -25px; right: 40px; transform: rotate(2deg); }

  /* Card áreas */
  .glass-cases-card { align-self: center; border-radius: 22px; padding: 14px 18px; cursor: pointer; overflow: hidden; }
  .cases-min-header { display: flex; align-items: center; justify-content: space-between; }
  .cases-title-text { font-size: 14px; font-weight: 800; color: #ffffff; text-shadow: 0 1px 2px rgba(0,0,0,0.35); }
  .search-icon-wrapper { background: rgba(255, 255, 255, 0.18); border-radius: 50%; padding: 6px; display: flex; color: #ffffff; }
  .glass-cases-card .card-expand-body { margin-top: 14px; padding-top: 0; border-top: none; }
  .quick-cases-list { list-style: none; display: flex; flex-direction: column; gap: 14px; }
  .quick-cases-list a { text-decoration: none; display: flex; flex-direction: column; font-size: 13.5px; transition: transform 0.3s var(--ease-ios); }
  .quick-cases-list a:hover { transform: translateX(4px); }
  .quick-cases-list strong { font-weight: 800; color: #ffffff; text-shadow: 0 1px 2px rgba(0,0,0,0.35); }
  .quick-cases-list span { font-size: 11.5px; color: rgba(255,255,255,0.82); margin-top: 2px; }

  /* ---------- MARQUEE ---------- */
  .marquee-section { width: 100%; background: #ffffff; padding: 15px 0; border-top: 1px solid var(--border-color); border-bottom: 1px solid var(--border-color); z-index: 5; position: relative; overflow: hidden; }
  .marquee-container { width: 100vw; max-width: 100%; overflow: hidden; display: flex; align-items: center; }
  .marquee-track { display: flex; gap: 60px; padding-right: 60px; width: max-content; animation: scroll-marquee 30s linear infinite; }
  .marquee-track img { height: 60px; width: auto; opacity: 0.4; filter: grayscale(100%); transition: all 0.3s ease; }
  .marquee-track img:hover { opacity: 1; filter: grayscale(0%); cursor: pointer; }
  @keyframes scroll-marquee { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }

  /* ---------- SECCIONES ---------- */
  .about-section, .projects-section, .services-section, .contact-section { position: relative; width: 100%; padding: 80px 32px; z-index: 5; display: flex; justify-content: center; }
  .contact-section { padding-bottom: 160px; background-color: #ffffff; }
  .about-container, .projects-container, .services-container, .contact-container { max-width: 900px; width: 100%; display: flex; flex-direction: column; align-items: center; }
  .projects-container { max-width: 1200px; }

  .sticky-note { position: relative; background-color: var(--pastel-yellow); padding: 10px 18px 6px; border-radius: 6px; box-shadow: 2px 4px 10px rgba(0,0,0,0.05); transform: rotate(-3deg); margin-bottom: 24px; display: flex; align-items: center; justify-content: center; }
  .real-pin { position: absolute; top: -12px; width: 24px; height: 24px; filter: drop-shadow(0px 2px 2px rgba(0,0,0,0.2)); }
  .note-text { font-weight: 800; font-size: 13px; color: var(--navy-dark); text-transform: uppercase; letter-spacing: 0.05em; }
  .about-header, .section-header { text-align: center; margin-bottom: 40px; }
  .about-header h2, .section-header h2 { font-size: 3.5rem; font-weight: 900; line-height: 1.1; letter-spacing: -0.03em; color: var(--navy-dark); margin-bottom: 16px; }
  .about-subtitle, .section-header p { font-size: 1.4rem; line-height: 1.6; color: var(--text-muted); max-width: 800px; margin: 0 auto; font-weight: 500; }

  .mac-window { width: 100%; background: var(--bg-soft); backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px); border: 1px solid var(--border-color); border-radius: 16px; box-shadow: 0 20px 40px rgba(0, 0, 0, 0.06); overflow: hidden; }
  .mac-titlebar { background: rgba(241, 245, 249, 0.8); padding: 14px 20px; display: flex; align-items: center; border-bottom: 1px solid var(--border-color); }
  .mac-buttons { display: flex; gap: 8px; }
  .mac-dot { width: 12px; height: 12px; border-radius: 50%; }
  .mac-dot.red { background-color: #ff5f56; border: 1px solid #e0443e; }
  .mac-dot.yellow { background-color: #ffbd2e; border: 1px solid #dea123; }
  .mac-dot.green { background-color: #27c93f; border: 1px solid #1aab29; }

  .mac-content { display: grid; grid-template-columns: 280px 1fr; gap: 40px; padding: 40px; align-items: center; }
  .bio-image-col { display: flex; justify-content: center; }

  .animated-memoji { animation: slideUpFade 1.2s var(--ease-out) 0.5s forwards; opacity: 0; transform: translateY(40px); }
  @keyframes slideUpFade { to { opacity: 1; transform: translateY(0); } }

  .memoji-circle { width: 240px; height: 240px; border-radius: 50%; object-fit: cover; background-color: #fef08a; box-shadow: 0 10px 25px rgba(0,0,0,0.1); }
  .bio-info-col { display: flex; flex-direction: column; gap: 30px; }
  .bio-intro { font-size: 1.2rem; line-height: 1.6; color: var(--navy-dark); }
  .bio-intro strong { font-weight: 800; }

  .stats-grid-about { display: grid; grid-template-columns: repeat(2, 1fr); gap: 24px; }
  .stat-number { font-size: 2.5rem; font-weight: 900; margin-bottom: 4px; color: var(--navy-dark); letter-spacing: -0.04em; }
  .stat-item p { font-size: 15px; font-weight: 600; color: var(--text-muted); line-height: 1.3; }

  /* ---------- PORTAFOLIO POR ÁREAS ---------- */
  .area-nav { display: flex; gap: 10px; flex-wrap: wrap; justify-content: center; margin-bottom: 40px; }
  .area-nav button { display: flex; align-items: center; gap: 8px; padding: 10px 18px; border-radius: 30px; background: var(--bg-soft); border: 1px solid var(--border-color); text-decoration: none; color: var(--navy-dark); font-weight: 800; font-size: 14px; transition: background 0.3s ease, color 0.3s ease, transform 0.3s var(--ease-ios); }
  .area-nav button .accordion-icon { font-size: 18px; line-height: 1; transition: transform 0.35s var(--ease-ios); }
  .area-nav button { align-items: center; gap: 8px; padding: 10px 18px; border-radius: 30px; background: var(--bg-soft); border: 1px solid var(--border-color); text-decoration: none; color: var(--navy-dark); font-weight: 800; font-size: 14px; transition: background 0.3s ease, color 0.3s ease, transform 0.3s var(--ease-ios); cursor: pointer; font-family: inherit; }
  .area-nav button:hover, .area-nav button[aria-expanded="true"] { background: var(--navy-dark); color: #ffffff; transform: translateY(-2px); }
  .area-nav button[aria-expanded="true"] .accordion-icon { transform: rotate(45deg); }

  .area-window { max-width: 1200px; margin-bottom: 48px; backdrop-filter: none; -webkit-backdrop-filter: none; background: rgba(255,255,255,0.85); scroll-margin-top: 24px; }
  .area-accordion { margin-bottom: 14px; }
  .area-accordion .mac-titlebar { cursor: pointer; user-select: none; }
  .area-accordion .area-body { display: grid; grid-template-rows: 0fr; padding-top: 0; padding-bottom: 0; opacity: 0; transition: grid-template-rows 0.55s var(--ease-ios), opacity 0.35s ease, padding 0.55s var(--ease-ios); }
  .area-accordion .area-body > * { min-height: 0; overflow: hidden; }
  .area-accordion.is-open .area-body { grid-template-rows: 1fr; padding: 36px; opacity: 1; }
  .area-accordion.is-open .mac-titlebar { border-bottom-color: var(--border-color); }
  .area-accordion .area-titlebar-plus { margin-left: auto; font-size: 22px; font-weight: 700; transition: transform 0.35s var(--ease-ios); }
  .area-accordion.is-open .area-titlebar-plus { transform: rotate(45deg); }

  .area-window .mac-titlebar { justify-content: space-between; gap: 16px; }
  .window-label { background: white; padding: 6px 16px; border-radius: 20px; font-size: 12px; color: var(--text-muted); font-weight: 500; border: 1px solid var(--border-color); flex-grow: 1; max-width: 420px; text-align: center; }
  .area-index { font-family: monospace; font-size: 13px; font-weight: 800; color: var(--text-muted); opacity: 0.5; }
  .area-body { padding: 36px; }

  .area-head { display: flex; align-items: flex-start; justify-content: space-between; gap: 20px; flex-wrap: wrap; margin-bottom: 22px; }
  .area-title { font-size: 2.2rem; font-weight: 900; letter-spacing: -0.03em; line-height: 1.1; color: var(--navy-dark); }
  .area-desc { font-size: 1.05rem; line-height: 1.6; color: var(--text-muted); font-weight: 500; max-width: 720px; margin-top: 10px; }
  .area-badge { padding: 8px 16px; border-radius: 30px; font-size: 12.5px; font-weight: 800; color: var(--navy-dark); white-space: nowrap; transform: rotate(2deg); }
  .badge-yellow { background: var(--pastel-yellow); }
  .badge-pink { background: var(--pastel-pink); }
  .badge-green { background: var(--pastel-green); }

  .area-tools { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 24px; }
  .area-tools span { font-size: 12px; font-weight: 700; padding: 6px 12px; border-radius: 20px; background: rgba(241,245,249,0.9); border: 1px solid var(--border-color); color: var(--navy-dark); }

  .area-notes { list-style: none; display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 14px; margin-bottom: 32px; }
  .area-notes li { padding: 16px 18px; border-radius: 14px; background: rgba(241, 245, 249, 0.6); border: 1px solid var(--border-color); }
  .note-company { display: block; font-size: 13px; font-weight: 900; color: var(--navy-dark); margin-bottom: 6px; }
  .area-notes p { font-size: 13.5px; line-height: 1.55; color: var(--text-muted); font-weight: 500; }

  /* Stack tecnológico */
  .stack-block { margin: 6px 0 28px; }
  .stack-title { font-size: 13px; font-weight: 800; color: var(--text-muted); margin-bottom: 12px; }
  .stack-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(118px, 1fr)); gap: 12px; }
  .stack-item { display: flex; flex-direction: column; align-items: center; gap: 10px; padding: 18px 10px 14px; border-radius: 16px; background: rgba(255,255,255,0.8); border: 1px solid var(--border-color); font-size: 13px; font-weight: 700; text-align: center; color: var(--navy-dark); transition: transform 0.3s var(--ease-ios), box-shadow 0.3s ease; }
  .stack-item:hover { transform: translateY(-4px); box-shadow: 0 14px 26px rgba(15,23,42,0.08); }
  .stack-item svg { width: 38px; height: 38px; }

  /* Collage de media (masonry con columnas CSS) */
  .media-grid { column-count: 3; column-gap: 20px; }
  .media-card { break-inside: avoid; -webkit-column-break-inside: avoid; page-break-inside: avoid; margin: 0 0 20px; background: #f8fafc; border: 1px solid var(--border-color); border-radius: 16px; padding: 12px; display: flex; flex-direction: column; gap: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.03); }
  .media-card video, .media-card img { width: 100%; border-radius: 10px; object-fit: cover; cursor: zoom-in; transition: transform 0.3s ease; display: block; }
  .media-card video:hover, .media-card img:hover { transform: translateY(-2px); }

  .vertical-vid { aspect-ratio: 9/16; background: #000; }
  .horizontal-vid { aspect-ratio: 16/9; background: #000; }
  .horizontal-img.w-full-img { aspect-ratio: auto; height: auto; max-height: 500px; object-fit: contain; background: transparent; }

  .media-caption { font-size: 13.5px; line-height: 1.4; text-align: center; padding: 0 4px 4px; }
  .cap-title { font-weight: 800; color: var(--navy-dark); }
  .cap-sep { margin: 0 4px; opacity: 0.35; }
  .cap-company { font-weight: 600; color: var(--text-muted); white-space: nowrap; }

  .media-link { background: #f1f5f9; color: var(--navy-dark); padding: 12px; border-radius: 8px; text-align: center; text-decoration: none; font-weight: 800; font-size: 14px; border: 1px solid var(--border-color); transition: all 0.3s ease; }
  .media-link:hover { background: var(--navy-dark); color: #ffffff; }
  .area-more-btn { display: block; width: fit-content; margin: 28px auto 0; padding: 12px 24px; border-radius: 999px; background: var(--navy-dark); color: #fff; text-decoration: none; font-size: 13px; font-weight: 800; transition: transform 0.3s ease, box-shadow 0.3s ease; }
  .area-more-btn:hover { transform: translateY(-2px); box-shadow: 0 10px 24px rgba(15,23,42,0.16); }
  .brand-logo-img { width: 38px; height: 38px; padding: 7px; border-radius: 10px; background: #fff; border: 1px solid var(--border-color); object-fit: contain; }
  .social-stack-block { margin-top: 4px; } 
  /* ---------- SERVICIOS ---------- */
  .services-grid-blocks { display: flex; flex-direction: column; gap: 14px; width: 100%; max-width: 800px; }
  .service-block { padding: 18px 24px; border-radius: 16px; backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px); border-width: 1px; border-style: solid; box-shadow: 0 10px 30px rgba(0,0,0,0.03); cursor: pointer; transition: transform 0.3s ease, box-shadow 0.3s ease; overflow: hidden; }
  .service-block:hover { transform: translateY(-2px); }
  .service-block-header { display: flex; justify-content: space-between; align-items: center; gap: 16px; }
  .service-block-header h3 { font-size: 1.2rem; font-weight: 800; color: var(--navy-dark); margin-bottom: 0; }
  .icon-plus { font-size: 1.5rem; font-weight: 800; color: var(--navy-dark); transition: transform 0.4s var(--ease-ios); flex-shrink: 0; }
  .service-block.open .icon-plus { transform: rotate(45deg); }
  .service-block-body { display: grid; grid-template-rows: 0fr; opacity: 0; transition: grid-template-rows 0.5s var(--ease-ios), opacity 0.4s ease; }
  .service-block-inner { overflow: hidden; min-height: 0; padding-top: 0; }
  .service-block.open .service-block-body { grid-template-rows: 1fr; opacity: 1; }
  .service-block.open .service-block-inner { padding-top: 14px; }
  .service-copy { display: grid; grid-template-columns: 90px 1fr; gap: 6px 14px; align-items: start; }
  .service-copy .flow-label { margin-top: 3px; }
  .service-copy p { font-size: 0.98rem; line-height: 1.5; color: var(--text-muted); margin: 0; }
  .service-whatsapp { display: inline-flex; margin-top: 16px; padding: 10px 16px; border-radius: 999px; background: var(--navy-dark); color: #fff; text-decoration: none; font-size: 12px; font-weight: 800; transition: transform 0.3s ease, opacity 0.3s ease; }
  .service-whatsapp:hover { transform: translateY(-2px); opacity: 0.9; }
  /* ---------- CONTACTO ---------- */
  .contact-container { display: grid; grid-template-columns: 1fr 1fr; gap: 0; padding: 0; }
  .light-gray-bg { background: #f8fafc; border-right: 1px solid var(--border-color); }
  .contact-left { padding: 50px; }

  .coffee-title { display: flex; align-items: center; gap: 12px; font-size: 2.4rem; font-weight: 900; margin-bottom: 16px; color: var(--navy-dark); letter-spacing: -0.02em; }
  .coffee-cup-svg { color: #8B5A2B; flex-shrink: 0; overflow: visible; }
  .coffee-liquid { fill: #8B5A2B; transform-box: fill-box; transform-origin: bottom; transform: scaleY(0); transition: transform 0.12s linear; }
  .steam { opacity: 0; stroke-width: 1.5px; transform: translateY(5px); transition: opacity 0.2s ease, transform 0.25s ease; }
  .coffee-section-active .steam { opacity: 0.6; transform: translateY(0); }

  .coffee-cta { display: inline-flex; align-items: center; justify-content: center; margin: 0 0 6px; padding: 13px 20px; border-radius: 999px; background: var(--navy-dark); color: #fff; text-decoration: none; font-size: 14px; font-weight: 800; transition: transform 0.3s ease, box-shadow 0.3s ease; }
  .coffee-cta:hover { transform: translateY(-2px); box-shadow: 0 10px 24px rgba(15,23,42,0.18); }

  .contact-left p { font-size: 1.1rem; line-height: 1.6; color: var(--text-muted); margin-bottom: 30px; font-weight: 500; }

  .contact-socials-row { display: flex; gap: 16px; margin-top: 20px; }
  .social-icon-btn { background: rgba(255,255,255,0.7); width: 54px; height: 54px; border-radius: 50%; display: flex; align-items: center; justify-content: center; color: var(--navy-dark); border: 1px solid var(--border-color); transition: all 0.3s; }
  .social-icon-btn:hover { background: white; transform: translateY(-4px); box-shadow: 0 10px 20px rgba(0,0,0,0.08); color: #000; }

  .contact-right { padding: 50px; }
  .contact-form { display: flex; flex-direction: column; gap: 20px; }
  .input-group { display: flex; flex-direction: column; gap: 8px; }
  .input-group label { font-size: 0.9rem; font-weight: 800; color: var(--navy-dark); text-transform: uppercase; }
  .input-group input, .input-group textarea { width: 100%; padding: 14px; border-radius: 8px; border: 1px solid var(--border-color); background: rgba(255,255,255,0.8); font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem; color: var(--navy-dark); outline: none; transition: border 0.3s; }
  .input-group input:focus, .input-group textarea:focus { border-color: var(--navy-dark); }

  .privacy-disclaimer { background: rgba(15, 23, 42, 0.8); padding: 16px; border-radius: 8px; margin-top: 10px; border: 1px solid rgba(0,0,0,0.2); }
  .privacy-disclaimer p { font-size: 0.85rem; color: rgba(255,255,255,0.8) !important; line-height: 1.5; margin: 0; }
  .privacy-link { color: #ef4444; text-decoration: underline; font-weight: 600; }
  .privacy-link:hover { color: #f87171; }
  .privacy-checkbox-group { display: flex; align-items: center; gap: 10px; margin-top: 10px; }
  .privacy-checkbox-group input { width: 18px; height: 18px; cursor: pointer; accent-color: var(--navy-dark); }
  .privacy-checkbox-group label { font-size: 0.9rem; color: var(--navy-dark) !important; font-weight: 600; cursor: pointer; }

  .submit-btn { background: var(--navy-dark); color: white; border: none; padding: 16px; border-radius: 8px; font-size: 1.1rem; font-weight: 800; cursor: pointer; transition: background 0.3s; margin-top: 10px; }
  .submit-btn:hover { background: #1e293b; }

  /* ---------- FOOTER ---------- */
  .black-footer { background: #000000; color: rgba(255,255,255,0.6); padding: 30px 20px 120px; text-align: center; font-size: 0.95rem; font-weight: 600; position: relative; z-index: 5; border: none; }

  .hidden-eyes-container { display: flex; justify-content: center; margin-bottom: 20px; opacity: 0.06; pointer-events: none; }
  .blinking-eyes { transform-origin: center; animation: subtle-blink 5s infinite; }
  @keyframes subtle-blink {
    0%, 92%, 96%, 100% { transform: scaleY(1); }
    94%, 98% { transform: scaleY(0.1); }
  }

  /* ---------- DOCK FLOTANTE · LIQUID GLASS ---------- */
  .floating-dock {
    position: fixed; bottom: 22px; left: 50%; transform: translateX(-50%);
    padding: 10px 20px 16px; border-radius: 34px; z-index: 9999; isolation: isolate;
    background: linear-gradient(180deg, rgba(255,255,255,0.46) 0%, rgba(255,255,255,0.16) 100%);
    -webkit-backdrop-filter: blur(22px) saturate(200%);
    backdrop-filter: blur(22px) saturate(200%);
    border: 1px solid rgba(255,255,255,0.55);
    box-shadow:
      inset 0 1.5px 0 rgba(255,255,255,0.95),
      inset 0 -1px 0 rgba(255,255,255,0.28),
      inset 0 0 18px rgba(255,255,255,0.28),
      0 0 0 0.5px rgba(15,23,42,0.10),
      0 18px 40px rgba(15,23,42,0.22),
      0 4px 10px rgba(15,23,42,0.10);
    animation: dock-in 0.9s var(--ease-out) 0.6s backwards;
  }
  .floating-dock::before { content: ""; position: absolute; inset: 0; border-radius: inherit; z-index: -1; pointer-events: none;
    background:
      radial-gradient(120% 90% at 18% 0%, rgba(255,255,255,0.6), rgba(255,255,255,0) 55%),
      linear-gradient(115deg, rgba(255,255,255,0) 38%, rgba(255,255,255,0.22) 50%, rgba(255,255,255,0) 62%); }
  @keyframes dock-in { from { opacity: 0; transform: translate(-50%, 40px); } to { opacity: 1; transform: translate(-50%, 0); } }

  .dock-list { display: flex; gap: 18px; list-style: none; margin: 0; padding: 0; align-items: center; }
  .dock-item { position: relative; display: flex; justify-content: center; }
  .dock-link { display: flex; align-items: center; }
  .mac-icon { width: 52px; height: 52px; object-fit: contain; transition: transform 0.35s var(--ease-ios); filter: drop-shadow(0 4px 6px rgba(15,23,42,0.18)); }
  .dock-item:hover .mac-icon { transform: scale(1.22) translateY(-8px); }
  .dock-item:hover + .dock-item .mac-icon,
  .dock-item:has(+ .dock-item:hover) .mac-icon { transform: scale(1.1) translateY(-3px); }

  /* Indicador de sección activa */
  .dock-item::after { content: ""; position: absolute; bottom: -10px; width: 4px; height: 4px; border-radius: 50%; background: rgba(15,23,42,0.75); opacity: 0; transform: scale(0.5); transition: opacity 0.3s ease, transform 0.3s var(--ease-ios); }
  .dock-item.active::after { opacity: 1; transform: scale(1); }

  .dock-item::before { content: attr(data-tooltip); position: absolute; top: -42px; background: rgba(15,23,42,0.88); -webkit-backdrop-filter: blur(8px); backdrop-filter: blur(8px); color: white; font-size: 12px; font-weight: 700; padding: 6px 12px; border-radius: 10px; opacity: 0; visibility: hidden; transition: all 0.2s; white-space: nowrap; pointer-events: none; }
  .dock-item:hover::before { opacity: 1; visibility: visible; top: -50px; }

  /* ---------- RESPONSIVE ---------- */
  @media (max-width: 1000px) {
    .media-grid { column-count: 2; }
  }

  @media (max-width: 900px) {
    .brand-centered { font-size: 16px; top: 20px; }

    /* En móvil, los hijos de la columna izquierda pasan a ser hijos directos del flex para poder reordenarlos */
    .hero-container { display: flex; flex-direction: column; gap: 16px; margin-top: 50px; min-height: auto; padding-bottom: 120px; }
    .left-col { display: contents; }
    .glass-profile-card { order: 1; }
    .hero-center-text { order: 2; margin-top: 10px; align-self: auto; }
    .glass-cases-card { order: 3; align-self: stretch; }
    .hero-quote-corner { order: 4; text-align: center; margin: 24px auto 0; max-width: 100%; }

    .hero-center-text h1 { font-size: 2.8rem; }
    .expand-bio { font-size: 14.5px; }
    .about-subtitle { font-size: 1.15rem; }

    .floating-pill { display: none; }
    .floating-dock { padding: 10px 16px 14px; }
    .mac-icon { width: 44px; height: 44px; }
    .dock-list { gap: 16px; }
    .about-section, .services-section { padding: 60px 20px; }
    .projects-section { padding: 40px 20px; }
    .contact-section { padding: 60px 20px 140px; }
    .about-header h2, .section-header h2, .coffee-title { font-size: 2.4rem; }
    .mac-content { grid-template-columns: 1fr; gap: 24px; padding: 24px; text-align: center; }
    .stats-grid-about { grid-template-columns: 1fr; gap: 16px; }
    .contact-container { grid-template-columns: 1fr; }
    .contact-left, .contact-right { padding: 30px; }
    .contact-left { border-right: none; border-bottom: 1px solid var(--border-color); }

    .marquee-track { gap: 40px; }
    .marquee-track img { height: 35px; }

    .area-body { padding: 20px; }
    .area-accordion.is-open .area-body { padding: 20px; }
    .area-title { font-size: 1.7rem; }
    .area-desc { font-size: 0.98rem; }
    .window-label { display: none; }
    .stack-grid { grid-template-columns: repeat(auto-fit, minmax(100px, 1fr)); }
    .media-card { padding: 8px; gap: 8px; }
    .media-caption { font-size: 12.5px; }
    .zoom-caption { font-size: 0.95rem; }

    .minimal-flow { flex-direction: column; align-items: flex-start; gap: 16px; }
    .flow-arrow { transform: rotate(90deg); margin: -4px 0; }
    .service-block { padding: 18px 20px; }
  }

  @media (max-width: 600px) {
    .media-grid { column-count: 1; }
    .media-card.bento-vertical { max-width: 340px; margin-left: auto; margin-right: auto; }
  }
</style>
